# AEGIS — pinned, lean dependency set.
# Rationale: no ML framework, no DB server, no JS build chain.
# The verifier, calibration fit and dashboard are all first-party code.
fastapi==0.115.6
uvicorn==0.34.0
pydantic==2.10.4
httpx==0.28.1
PyYAML==6.0.2
numpy==2.2.1
pytest==8.3.4
pytest-asyncio==0.25.0
